BASE_URL=
APP_URL=
EMAIL_ADDRESS=
PASSWORD=

# Only needed for the opencode backend (--cli opencode) — read by
# opencode.json's "amplify" provider via {env:AMPLIFY_API_TOKEN} to
# authenticate to the Amplify OpenAI proxy. See docs/opencode-adapter.md.
AMPLIFY_API_TOKEN=
